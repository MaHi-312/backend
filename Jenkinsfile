pipeline {
    agent { label 'workstation'}

    options {
        ansiColor('xterm')
    }

    stages {

      stage('compile code'){
        steps {
           echo 'CI'
        }
      }

      stage('Unit tests'){
        steps {
           echo 'CI'
        }
      }

      stage('Release'){
        steps {
           echo 'CI'
        }
      }
    }
}

////